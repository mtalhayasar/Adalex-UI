# Branch Protection CI
# Bu workflow main branch'e yapılan PR'ları kontrol eder

name: Branch Protection Check

on:
  pull_request:
    branches:
      - main
      - master

permissions:
  contents: read

jobs:
  pr-check:
    runs-on: ubuntu-latest
    name: PR Validation
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Validate PR
        run: |
          echo "🔍 PR Bilgileri:"
          echo "  - Hedef branch: ${{ github.base_ref }}"
          echo "  - Kaynak branch: ${{ github.head_ref }}"
          echo "  - PR açan: ${{ github.actor }}"
          echo ""
          
          # PR başlığının boş olmadığını kontrol et
          if [ -z "${{ github.event.pull_request.title }}" ]; then
            echo "❌ PR başlığı boş olamaz!"
            exit 1
          fi
          
          echo "✅ PR başlığı mevcut: ${{ github.event.pull_request.title }}"
          echo "✅ PR doğrulaması başarılı"
